# 镀膜伞具孔位板 CAD 配准运动

## 阶段技术评审 DR

| 项目 | 内容 |
|---|---|
| 文档编号 | DR-EIH-20260810-002 |
| 文档版本 | V2.0 |
| 评审日期 | 2026-08-10 |
| 当前阶段 | CAD 配准与原两阶段点云定位并行保留后的运动联调 |
| 评审结论 | 工程实现与实验运行已完成；生产放行条件尚未满足 |
| 评审人 | 待填写 |
| 负责人 | 待填写 |

> 本文是当前实现的阶段性 DR。原三孔、点云粗定位版本保留在 `DR_镀膜伞具三孔眼在手定位_阶段评审_V1.0.*`，作为历史记录，不再代表当前 CAD 运动流程的结论。

## 0. 本次版本的评审结论

当前项目同时保留两条孔位定位路径。本 DR 重点评审 CAD 配准运动路径，但原来的多段位移/点云两阶段路径仍可通过开关选择，用于兼容、对照和回退。CAD 路径能够完成以下闭环：

1. 从 STEP 生成并加载 11 个顶面大孔的 CAD 中心、半径/直径、顶部高度和 `+Z` 法向。
2. 在每次真实运动开始前重新采集 RGB，并自动完成 CAD 与相机的孔位匹配；匹配不唯一时要求人工确认，不任意采用对称解。
3. 通过平面 PnP/IPPE、畸变投影、正深度和法向方向检查求出 `T_camera_cad`。
4. 回到选定孔组的共同视野位置，在约 340 mm 处一次性采集深度，用于共同高度修正；深度不再提供孔中心、孔号或法向。
5. 在约 260 mm 处对每个选定孔采集 RGB/YOLO 帧，CAD 锁定目标中心、法向和孔号，YOLO 只用于最终 XY 精修；保留现有 ChArUco TCP-XY 补偿。
6. 执行最终 Z、工具/夹爪偏置和基坐标 `+Y 0.3 mm`，并将 CAD、YOLO、精定位和运动结果写入报告。

原有非 CAD 路径仍能完成“初始选孔 → 340 mm 粗定位 → 点云中心/法向闭环 → 260 mm RGB 精定位 → 最终目标点”的多段位移流程；该路径不使用 CAD 孔号来确定孔位，而是保留原来的 YOLO 选孔、RGB-D 局部点云和平面几何计算。

最新一次实验运行已经实际发出了机器人运动请求并完成 3 个选定孔的流程，但使用的是未通过生产质量门的手眼候选，报告状态为 `completed_experimental_handeye`。因此，本版本的结论是“功能链路已打通、实验运行通过；生产验收未通过”。

## 1. 评审范围与变更目标

### 1.1 目标

本阶段的目标是让 CAD 路径替换原有点云粗定位在 CAD 模式下的职责，同时保留原路径：

- 原方式：用点云拟合孔中心、平面和法向，再通过 340 mm 粗定位闭环寻找目标。
- 当前 CAD 方式：用 CAD 孔模型与 RGB 图像配准得到 CAD 坐标系到相机坐标系的变换，再由 CAD 孔中心和法向直接规划目标位姿。
- 保留方式：在非 CAD 模式下，继续用初始 YOLO 选孔和 RGB-D 局部点云确定孔中心、平面和法向，并沿用逐孔多段位移。

在 CAD 模式下，点云不承担孔中心、法向和 CAD 孔号识别职责；深度只在选定孔组的共同视野位置采集，用于补偿当前工件相对 340 mm 参考高度的 Z 偏差。在非 CAD 两阶段模式下，点云仍然承担每个孔的粗中心、局部平面和法向计算。

### 1.2 两条路径的边界

| 项目 | CAD 运动路径 | 原两阶段点云路径 |
|---|---|---|
| 模式开关 | `CAD_MOTION_MODE=True` 或 `--cad-motion` | `CAD_MOTION_MODE=False` 且 `TWO_STAGE_MODE=True`，或 `--two-stage-hole-localization` |
| 孔位来源 | CAD 11 孔模型与 RGB 配准 | 初始 YOLO 选孔与 RGB-D 点云几何 |
| 340 mm | 选定孔组共同视野，一次采集共享高度 | 每个孔分别到 340 mm，点云拟合并做粗定位闭环 |
| 260 mm | CAD 规划目标位姿，RGB/YOLO 只修最终 XY | 点云平面确定 Z，RGB/YOLO 求精定位中心 |
| 法向来源 | CAD 顶面 `+Z` 法向经位姿变换 | 340 mm 局部点云平面法向 |
| 报告目录 | `data\cad_motion_runs\cad-motion-*` | `data\hole_localization_runs\two-stage-*` |

当前 `run_hole_localization_pycharm.py` 默认设置 `CAD_MOTION_MODE=True`；主脚本参数默认仍为 `CAD_MOTION=False`、两阶段模式开启。因此直接运行主脚本或 GUI 旧流程时，旧的多段位移功能仍然会被调用，不能把它误判为删除。

真实 CAD 运动的启动顺序为：连接运动会话 → 回到保存的原点/观察位 → 等待机器人稳定 → 在原点处采集 5 帧 RGB 做实时 CAD 配准 → 选择目标孔。预览模式不自动移动机器人；如果预览时当前位置看不到孔位板，应先人工把机器人放到保存的观察位。

### 1.3 本版本不包含的结论

- 未将当前实验手眼结果认定为生产级手眼标定。
- 未完成全部 11 孔逐孔运动的现场重复性验收。
- 未用一次实验运行证明夹爪插入、接触和取放动作的最终机械精度。
- 未将旧两阶段点云路径的历史实验结果直接等同于当前 CAD 路径的生产验收结果。

## 2. 当前系统基线

### 2.1 硬件和坐标约定

| 项目 | 当前基线 |
|---|---|
| 机器人 | AUBO i5H，使用现有 AUBO SDK |
| 相机 | Orbbec Gemini 435Le；CAD 配准阶段只读取 RGB，340 mm 高度采集阶段读取 RGB-D |
| 检测模型 | 当前 GUI/运行配置中的 YOLO 模型，输出大孔检测框；第一阶段孔中心取框中心 |
| CAD 来源 | `C:\MM\aubo_tools\aubo_workbench_project\孔位板_JXDZ26-KWB-001.STEP` |
| CAD 模型 | `C:\MM\aubo_tools\data\cad_model\cad_hole_model.json` |
| CAD 单位 | mm |
| CAD 顶面 | `Z=6 mm` |
| CAD 顶面法向 | `+Z` |
| 相机坐标 | 使用现有 RGB 内参和畸变参数进行投影/PnP |
| 工具坐标约定 | 当前项目约定 `X` 正方向向前，`Y` 正方向向左；最终基坐标 `+Y 0.3 mm` 保留 |

CAD JSON 的当前模型版本为 `cad_hole_model_v1`，包含 11 个顶面大孔。正式运行前仍需要检查 STEP、JSON 和当前实物是否属于同一块孔位板。

### 2.2 主要入口

| 入口 | 用途 | 当前状态 |
|---|---|---|
| `C:\MM\aubo_tools\aubo_workbench_project\run_workbench.py` | 主 GUI，进入孔洞定位、CAD 预览、CAD 运动和夹爪控制 | 当前推荐入口 |
| `C:\MM\aubo_tools\aubo_workbench_project\run_cad_registration_preview.py` | CAD 配准预览、映射确认、5 帧质量统计和叠加图输出 | 阶段 0，不应运动 |
| `C:\MM\aubo_tools\aubo_workbench_project\run_yolo_eye_in_hand_optimized.py` | CAD 运动流程核心实现及原两阶段点云流程 | 由 GUI/直接入口调用 |
| `C:\MM\aubo_tools\aubo_workbench_project\run_hole_localization_pycharm.py` | 直接运行配置入口 | 默认只预览；真实运动和实验手眼仍需显式覆盖，使用前必须复核 |
| `C:\MM\aubo_tools\aubo_workbench_project\aubo_workbench\gripper_control.py` | 独立夹爪控制窗口 | 已接入 GUI，不作为 CAD 注册放行条件 |

当前运行环境仍为 conda 的 `lip_env310` 环境。

## 3. 当前实现架构

### 3.1 CAD 模型加载

`cad_model.py` 支持从已有 JSON 恢复 `CadModel`，并校验孔数量、单位、顶面高度、法向以及必要字段。默认模型为 11 个顶面孔。模型中每个孔至少包含：

- CAD 孔号；
- CAD 三维中心；
- 半径/直径；
- 顶部高度；
- 顶面法向。

### 3.2 CAD 到相机的配准

`cad_registration.py` 完成以下工作：

1. 从 YOLO 检测结果读取孔框中心。
2. 在满足几何约束的前提下，为可见检测孔生成 CAD 候选匹配。
3. 检测到的候选不唯一时，进入人工确认或使用已保存映射；禁止在对称孔位之间任意选解。
4. 使用至少 4 个有效对应点进行平面 PnP/IPPE。
5. 使用 RGB 内参和畸变参数计算 `T_camera_cad`，并检查正深度、法向朝向和重投影质量。
6. 投影全部 11 个 CAD 孔，绘制 CAD 圆、孔号、YOLO 框、残差矢量和误差统计。

预览入口采集 5 帧，要求至少 3 帧通过质量门，然后以有效帧的孔中心中位数重新求解。预览结果的 `motion_allowed` 固定为 `False`；即使配准质量通过，阶段 0 也只允许生成结果，不直接连接运动流程。

### 3.3 CAD 运动流程

真实运动路径在每次启动运动时都重新执行一次实时 CAD 配准，策略为 `fresh_live_auto_each_run`，不复用上一次报告里的旧位姿。流程如下：

1. 连接机器人并回到保存的原点/安全位置。
2. 在原点位置选择本次要处理的 CAD 孔组。
3. 根据所选孔的 CAD 中心和法向规划共同视野位置，使选定孔尽可能同时处于 RGB-D 视野中心附近。
4. 到共同视野位置采集一次深度。单孔时用单孔高度；选定 2～3 孔时用选定孔共享高度；选定不少于 4 孔时使用至少 4 个有效孔进行共享高度估计。这样不需要对每个孔重复 340 mm 深度采集。
5. 对每个选定孔移动到 CAD 计算的约 260 mm 精定位位姿。
6. 在 260 mm 位置丢弃稳定等待帧后采集 RGB/YOLO 帧，并用 CAD 投影附近的检测作为精定位候选。
7. CAD 继续提供孔中心、法向、孔号和目标 Z；YOLO 只计算最终 XY 修正，并经过现有 ChArUco TCP-XY 补偿。
8. 执行最终 Z、工具/夹爪偏置和基坐标 `+Y 0.3 mm`，保存当前孔的目标位姿、实际 TCP、XY、Z 和 Y trim 记录。
9. 每个选定孔完成后由操作员确认是否继续下一个孔。

### 3.4 原有非 CAD 两阶段流程（仍可使用）

非 CAD 路径在代码中由 `run_two_stage_hole_localization()` 保留，GUI 中对应旧流程的“开始两阶段定位”。它的逐孔多段位移和孔位确定过程如下：

1. 在初始 RGB-D 画面中选择一个或多个孔，记录初始 YOLO 身份和点云三维投影。
2. 对当前孔先做安全抬升/横移，再下降到该孔约 340 mm 粗定位位。
3. 用 YOLO 框外环深度点拟合局部平面，计算 YOLO 中心射线与点云平面的交点、局部平面点和孔面法向。
4. 根据中心偏差、法向偏差执行最多两次粗定位闭环校正；每个孔的粗定位数据写入 `coarse_captures`。
5. 以当前孔的点云平面为基准，必要时分次只修正基坐标 Z，到达约 260 mm。
6. 260 mm 阶段只使用 RGB/YOLO 多帧精定位，检测身份锁定在粗定位点云投影锚点附近；最终中心由 RGB 中心与粗阶段平面求交得到。
7. 最终仍执行 ChArUco TCP-XY 修正、最终 Z、工具/夹爪偏置和基坐标 `+Y 0.3 mm`。
8. 当前孔完成后等待输入 `m`，再开始下一个孔；未通过精定位质量门的孔会记录为 deferred，不执行最终目标点动作。

这条路径的报告会保留 `pointcloud_center_base_mm`、`pointcloud_center_camera_mm`、`coarse_normal_toward_camera_base`、`coarse_plane_rmse_mm`、`coarse_captures` 和 `fine_z_source=coarse_front_surface_plane_intersection_z` 等字段，便于和 CAD 路径进行对照。它不是死代码，也不是 CAD 配准报告的组成部分。

### 3.5 GUI 与夹爪

主 GUI 目前已将 CAD 配准预览、CAD 目标预览、CAD 260 mm 真实运动、精定位可视化和结果目录操作放入孔洞定位页面。夹爪控制从主界面打开一个独立的非模态窗口，主 GUI 仍可继续操作；夹爪通信由 `JiaZhua\z_erg_20c.py` 驱动。夹爪窗口目前提供串口、从站、波特率、超时、重试、夹持速度/电流/位置和旋转速度/电流/角度参数。

## 4. 质量门和放行规则

| 环节 | 当前门限/规则 | 失败处理 |
|---|---|---|
| CAD 单帧匹配 | 至少 4 个有效对应孔；正深度；法向方向正确 | 该帧不进入求解 |
| CAD 单帧重投影 | RMSE ≤ 2 px，最大误差 ≤ 4 px | 配准失败，不进入运动 |
| CAD 多帧稳定性 | 采集 5 帧，至少 3 帧有效；中心 P95 ≤ 1.5 mm | 本次注册失败 |
| 映射唯一性 | 对称/多解必须人工确认或读取已确认映射 | 禁止自动任意选解 |
| 340 mm 共享深度 | 默认 8 帧，至少 5 帧有效；平面 RMSE ≤ 3.5 mm；高度散布 P95 ≤ 2.0 mm | 不执行该孔组 |
| 260 mm 精定位 | 默认 12 帧，至少 6 帧有效；丢弃 10 帧稳定帧；中心散布 P95 ≤ 1.5 px；CAD-YOLO 匹配距离 ≤ 70 px | 不执行该孔 |
| 手眼 | 生产必须使用已验证手眼；实验手眼只能显式覆盖 | 生产模式阻止运动 |
| 阶段 0 预览 | `motion_allowed=False` | 只输出原图、叠加图、映射和 JSON |

质量门全部通过只表示视觉/几何条件通过，不代表机器人已经完成机械精度验收。特别是当前 260 mm 的 CAD-YOLO 平均像素距离属于诊断信息，不是单独的运动放行门；仍需结合中心稳定性、实际 TCP 记录和现场孔边缘覆盖情况验收。

## 5. 最近一次实验运行证据

数据目录：`C:\MM\aubo_tools\data\cad_motion_runs\cad-motion-20260807_165112`

### 5.1 CAD 配准和 340 mm 共享深度

| 项目 | 结果 |
|---|---:|
| CAD 自动注册有效帧 | 5/5 |
| 跨帧中心 P95 | 约 0.297 mm |
| CAD 注册失败原因 | 无 |
| 阶段 0 `motion_allowed` | `False`（预览模块的固定安全规则） |
| 选定孔 | CAD-09、CAD-06、CAD-10 |
| 共享深度有效帧 | 8/8 |
| 共享深度中位高度 | 336.452 mm |
| 相对 340 mm 的高度偏移 | -3.548 mm |
| 共享 Z 修正记录 | 约 +3.548 mm |
| 共享深度状态 | `passed` |

这说明“以选定孔组为共同视野，在 340 mm 一次采集深度”的代码路径已经运行起来；它不是以某一个孔单独的 340 mm 结果代表全部孔。

### 5.2 260 mm 精定位结果

| 孔 | 有效帧 | 中心散布 P95 | CAD-YOLO 平均距离 | 最终 XY 修正 mm |
|---|---:|---:|---:|---:|
| CAD-09 | 12/12 | 0.14 px | 4.92 px | `[0.4996, 2.3816]` |
| CAD-06 | 12/12 | 0.22 px | 2.64 px | `[0.4866, 2.5215]` |
| CAD-10 | 12/12 | 0.15 px | 4.44 px | `[0.6192, 2.4189]` |

本次报告的总体状态为 `completed_experimental_handeye`，且 `motion_requested=True`、`motion_executed=True`。运行使用的手眼候选仍为 `handeye_validated=False` 并启用了实验覆盖，因此这组数据只能作为功能联调证据，不能作为生产精度承诺。

## 6. 位姿和高度计算说明

### 6.1 CAD 坐标到基坐标

初始位置完成 CAD 注册后，代码按以下关系冻结当前工件的 CAD 位姿：

```text
T_base_cad = T_base_camera_initial @ T_camera_cad
```

之后每个选定孔的基坐标中心和法向由 CAD 模型转换得到。工件一次运行中不移动时，不需要每个孔重新示教 CAD；每次新的运动运行会重新做一次当前相机画面的自动匹配并立即应用。

### 6.2 340 mm 高度和 260 mm 精定位的关系

340 mm 是深度可靠的共同观测高度，不是某一个孔的最终插入高度。系统在共同视野位置采集深度，得到选定孔组的共享高度偏移，并把该偏移用于后续 CAD 目标 Z。

260 mm 是每个孔的精定位观测高度。到达 260 mm 后，RGB/YOLO 用来计算最终 XY 修正；最终 Z 仍由 CAD 顶面高度和 340 mm 深度修正共同确定。当前代码不是简单地“把 340 mm 减去 80 mm”，而是先用 CAD 位姿规划 260 mm 目标，再用共同深度结果修正 Z。

### 6.3 最终偏置

- 保留当前最终 Z 逻辑。
- 保留现有夹爪/工具偏置配置；代码支持夹爪目标模式和法向目标模式。
- 保留现有 ChArUco TCP-XY 补偿模型，但本次运行未单独形成 ChArUco 精度验收结论。
- 最终基坐标 `+Y 0.3 mm` 在 Z 动作之后执行。

## 7. 当前代码和数据追溯

| 类别 | 文件/目录 | 作用 |
|---|---|---|
| CAD 模型恢复 | `aubo_workbench/cad_model.py` | 校验并恢复 CAD JSON |
| CAD 配准 | `aubo_workbench/cad_registration.py` | 匹配、PnP/IPPE、质量门、投影和残差 |
| CAD 预览 | `run_cad_registration_preview.py` | RGB 预览、映射确认、5 帧报告；不运动 |
| CAD 运动 | `run_yolo_eye_in_hand_optimized.py` | 340 mm 共享深度、260 mm 精定位、运动和报告 |
| GUI | `run_workbench.py`、`aubo_workbench/gui_hole_localization.py` | CAD 预览、运动按钮、精定位可视化和结果目录 |
| 夹爪窗口 | `aubo_workbench/gripper_control.py` | 独立夹爪控制窗口 |
| 夹爪驱动 | `C:\MM\aubo_tools\JiaZhua\z_erg_20c.py` | 串口/Modbus 夹爪控制 |
| CAD 模型 | `C:\MM\aubo_tools\data\cad_model\cad_hole_model.json` | 11 孔模型 |
| 最新运动报告 | `C:\MM\aubo_tools\data\cad_motion_runs\cad-motion-20260807_165112\report.json` | 当前实验运行证据 |
| 最新注册报告 | `...\cad_registration_auto\cad_registration_report.json` | 运动启动时的实时 CAD 注册证据 |
| 当前追溯摘要 | 本 DR 第 1、4、5、7、8、9 节 | 旧版三孔追溯矩阵已删除；当前 CAD 与非 CAD 双路径的实现、质量门和证据边界以本 DR 及 README 为准 |

项目当前离线测试套件为 182 项，最近一次整套运行通过。测试通过只说明代码回归正常，不替代真实机器人、相机和手眼的现场验收。

## 8. 当前问题、风险和限制

### 8.1 手眼质量门尚未达标

最新实验报告明确记录 `handeye_validated=False`。当前直接运行配置 `run_hole_localization_pycharm.py` 已改为默认不执行运动、不允许实验手眼；但仍保留显式覆盖开关，操作员不能把该入口当作生产安全配置。正式放行前应：

1. 使用独立验证数据重新完成手眼候选筛选；
2. 将生产入口改为“未验证手眼禁止运动”；
3. 在 GUI 中显示并强制确认手眼质量状态；
4. 记录验证报告编号、采集日期和适用相机/工具配置。

### 8.2 现场精度证据不足

当前运行只处理了 CAD-09、CAD-06、CAD-10 三个孔。虽然 5 帧注册、8 帧共享深度和每孔 12 帧精定位均满足当前代码门限，但还缺少以下证据：

- 同一工件多次重新摆放后的 CAD 自动匹配稳定性；
- 11 孔或代表性孔组的重复运动结果；
- CAD 投影在真实孔边缘上的现场覆盖记录；
- 末端工具/夹爪进入孔位后的实际机械误差；
- 光照、反光、遮挡、漏检和对称匹配异常下的拒绝行为。

### 8.3 旧路径和配置一致性

旧点云粗定位代码仍保留，供兼容、对照和回退使用；它包含原来的多段安全位移、每孔 340 mm 粗定位、点云中心/法向闭环、260 mm RGB 精定位和逐孔目标点运动。CAD 路径不调用这套点云粗定位结果，但两条路径共享机器人运动、ChArUco XY、最终 Z、工具偏置和 `+Y 0.3 mm` 等部分底层逻辑。

后续整理必须明确区分 `CAD_MOTION_MODE=True/False` 的报告和质量门，不能把一条路径的孔号、中心或高度证据混入另一条路径。

## 9. 下一阶段验收计划

按以下顺序推进，不满足前一项时不扩大运动范围：

1. 在不运动模式下连续采集多次 RGB，确认 CAD 11 孔投影、孔号和 YOLO 框稳定重合。
2. 更换孔位板摆放位置，验证每次运动开始前的自动匹配能重新建立正确映射。
3. 使用已验证手眼重新做同样的 340 mm 共享深度和 260 mm 精定位实验。
4. 先开放单孔，再开放 2～3 孔共享高度，最后开放 4 个及以上孔的共享高度。
5. 对每个阶段记录目标 TCP、实际 TCP、最终 XY、最终 Z、`+Y 0.3 mm` trim 和夹爪动作结果。
6. 完成独立的重复性、漏检/误检、对称解和异常停止验收后，才允许切换为生产配置。

生产放行条件建议至少包括：手眼验证通过；CAD 配准、340 mm 深度和 260 mm 精定位质量门连续通过；现场机械末端误差达到项目要求；所有异常都能停止运动并保留报告。

## 10. 评审结论

本阶段已经完成 CAD 模型加载、RGB 自动配准、映射确认、全孔投影、共同视野深度采集、260 mm YOLO XY 精定位、ChArUco 补偿接口、最终 Z/工具偏置/`+Y 0.3 mm` 运动链路以及 GUI 结果展示。最近一次实验运行证明了“每次运动前自动匹配并直接应用”的链路可执行。

但由于当前手眼仍未通过生产质量门，且缺少多次摆放、全孔/多孔组和末端机械精度数据，本 DR 不能给出生产放行结论。下一阶段应优先关闭手眼和现场重复性两个风险，再扩大孔组和运动范围。

---

**评审状态：工程实现完成，实验联调通过，生产验收待完成。**
